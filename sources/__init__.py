"""데이터 소스 패키지.

교체 규칙:
  - 각 get_*() 함수의 '리턴 형태(str)'를 유지하면서 내부만 실제 API로 교체.
  - main.py 는 건드리지 않는다.
  - 소스 하나가 실패해도(예외/빈값) 브리핑 전체가 깨지지 않도록
    main.py 의 safe() 가 감싼다. 각 소스는 자기 안에서도 최대한 fallback 처리.
"""
